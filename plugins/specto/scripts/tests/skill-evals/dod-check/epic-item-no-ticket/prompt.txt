Run the specto dod-check skill in --mode=epic-creation for epic TOY-1. The spec folder is docs/development/specs/2026-01-01-canned-replies (its .specto-meta.yml links epic TOY-1). Fetch the epic's Issue Checklist and its child tickets via acli (it is on PATH) — the child-ticket lookup is `acli jira workitem search --jql "parent = TOY-1"`. For each DoD checklist item, check whether any child ticket references it. This is a read-only coverage report — do not create or modify anything in Jira or GitLab. Print the epic-creation DoD coverage report.
