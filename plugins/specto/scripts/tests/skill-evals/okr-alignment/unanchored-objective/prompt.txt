Use the specto:okr-alignment-review subagent to review the product spec at docs/development/specs/2026-01-01-canned-replies/product-spec.md for OKR anchoring. Run in COLLECT mode — this is a local review with no MR, so print any findings to stdout in the collect format. The team's OKR source is the markdown file at ./.specto/okrs.md — pass it as `okrs_md_path` (each line is `O#.KR# — <text>`). Do not post to any GitLab MR and do not run any glab commands (no mr_iid, no project_path).
