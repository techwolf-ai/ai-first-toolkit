Use the specto plan-to-tickets skill in DRY RUN on the plan at .specto/plan.md (epic TOY-1, spec folder docs/development/specs/toy). This is a dry run only — do NOT create anything in Jira and do NOT run any live acli writes. The two plan tasks are both trivially small and the same theme (config-key tweaks on the same team-settings record), so they should bundle into a single MR-sized ticket that keeps each task as its own acceptance-criteria line. Show the dry-run output.
