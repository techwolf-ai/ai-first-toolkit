Use the specto:scope-review subagent to review the product spec at docs/development/specs/2026-01-01-canned-replies/product-spec.md for scope discipline. Run in COLLECT mode — this is a local review with no MR, so print the findings to stdout in the collect format (grouped by section, one bullet per finding with its `[finding-type] line N` and a `*Fix:*`). Do not post to any GitLab MR and do not run any glab commands.
