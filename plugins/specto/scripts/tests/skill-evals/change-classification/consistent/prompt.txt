Use the specto:change-classification-review subagent to review the spec at docs/development/specs/2026-01-01-canned-replies/product-spec.md for change-classification consistency with its linked Jira epic. The spec folder's .specto-meta.yml links epic TOY-1. Resolve the epic's classification fields via the vetted helper "$CLAUDE_PLUGIN_ROOT/scripts/tracker/epic-fields.sh" TOY-1 (acli is on PATH). Run in COLLECT mode — print any findings to stdout in the collect format. Do not post to any GitLab MR and do not run any glab commands (no mr_iid, no project_path).
